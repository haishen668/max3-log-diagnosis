interface:
  display_name: "Max3 日志诊断"
  short_description: "解密并索引 CPE-MAX3/IC5980 大型日志，输出脱敏证据链"
  default_prompt: "Use $max3-log-diagnosis to decrypt, index, and analyze IC5980/MAX3 diagnostic packages with a redacted, reviewable evidence chain."

policy:
  allow_implicit_invocation: true
